from django.urls import path

from objects.views import add_object#, object_detail

urlpatterns = [
    # path('object/<slug:slug>/', object_detail, name='object_detail'),
    # path('add_object/', add_object(), name='add_object'),
]